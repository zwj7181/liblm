import { BaseTableOld } from '@lm_fe/components_m';
import React from 'react';
export default class ReserveTable extends BaseTableOld {
  renderTableConfig = () => {
    return <></>;
  };
  renderTitle = () => {
    return <></>;
  };
}
